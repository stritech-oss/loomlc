@AGENTS.md

## Claude Code

- `.claude/settings.json` turns off Claude Code's commit and PR attribution and denies the git and
  `gh` commands the rules above forbid. Don't override either in `.claude/settings.local.json`.
